{
  "name": "valkyria-ecm",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "echo 'No root build'",
    "heroku-postbuild": "cd frontend && npm install && npm run build"
  }
}
