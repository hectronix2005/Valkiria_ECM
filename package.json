{
  "name": "valkyria-ecm",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "echo 'No root build'",
    "heroku-postbuild": "cd frontend && npm install --include=dev && npm run build"
  }
}
